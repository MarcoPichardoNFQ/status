name: Modificar JSON

on:
  push:
    branches:
      - main

  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-json:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Instalar dependencias Python
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Ejecutar script 1
        run: python scripts/1.py

      - name: Ejecutar script 2
        run: python scripts/2.py

      # --- NUEVO PASO AÑADIDO AQUÍ ---
      - name: Inyectar Timestamp en HTML
        run: |
          TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
          sed -i "s/<span id=\"deploy-timestamp\">.*<\/span>/<span id=\"deploy-timestamp\">$TIMESTAMP<\/span>/g" main.html
      # -------------------------------
      # --- NUEVO PASO AÑADIDO AQUÍ ---

      - name: Commit cambios
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add .

          if ! git diff --cached --quiet; then
            git commit -m "Actualizacion automatica JSON"
            git push
          fi
